class User {
    constructor(username, displayName) {
        /*constructor: the method that gets called to create new instance of an object in the class*/
        this.username = username;
        this.displayName = displayName;
    }
}

export default User;